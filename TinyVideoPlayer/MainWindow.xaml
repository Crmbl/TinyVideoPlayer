<Window x:Class="TinyVideoPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vlc="clr-namespace:Vlc.DotNet.Wpf;assembly=Vlc.DotNet.Wpf"
        Style="{StaticResource WindowStyle}"
        mc:Ignorable="d">
    <Grid x:Name="MainGrid">
        <Grid x:Name="MediaButtons">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="TakeWidthButton" Height="45" Width="45" Panel.ZIndex="110" Background="Yellow" />
            <Button Grid.Column="1" x:Name="TakeHeightButton" Height="45" Width="45" Panel.ZIndex="110" Background="Red" />
            <Button Grid.Column="2" x:Name="ResizeButton" Height="45" Width="45" Panel.ZIndex="110" Background="Blue" />
        </Grid>
        <Canvas x:Name="DropZone" Style="{StaticResource CanvasStyle}" Visibility="Hidden"/>
        <vlc:VlcControl x:Name="VideoControl" Style="{StaticResource VlcStyle}" Visibility="Hidden" />
    </Grid>
</Window>
